/** Оболонка контенту всередині {@link AuthPlansBackdropComponent} для реєстрації / превʼю оплати. */
export const REGISTER_AUTH_SHELL_CLASS =
  'max-w-[min(100%,520px)] w-full items-center justify-center overflow-visible py-4 sm:py-6';
