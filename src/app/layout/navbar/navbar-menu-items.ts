export interface NavbarMenuItem {
  id: string;
  label: string;
  route: string;
}

// Kept empty intentionally to preserve current navbar behavior.
export const NAVBAR_MENU_ITEMS: NavbarMenuItem[] = [];
